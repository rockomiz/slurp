Dependencies:
 Qt >= 4.7.3

For Debian, if you install these packages you should be OK:

$ sudo aptitude install  libqtwebkit-dev qt4-dev-tools  

Linux: 

$ qmake
$ make 

Windows:

$ qmake
$ nmake 

Builds successfully on all platforms that Qt supports.
The resulting binary will be located at bin (/|\) slurp.
